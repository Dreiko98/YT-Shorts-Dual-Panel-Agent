events {
    worker_connections 1024;
}

http {
    upstream yt_shorts_backend {
        server yt-shorts-agent:8081;
    }

    server {
        listen 80;
        server_name _;

        # Redirigir a HTTPS si está configurado
        # return 301 https://$server_name$request_uri;

        location / {
            proxy_pass http://yt_shorts_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket support si es necesario
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location /health {
            proxy_pass http://yt_shorts_backend/health;
        }

        # Servir archivos estáticos directamente
        location /static {
            alias /app/static;
            expires 1d;
        }

        location /outputs {
            alias /app/outputs;
            expires 1h;
        }
    }

    # Configuración SSL (descomenta si tienes certificados)
    # server {
    #     listen 443 ssl;
    #     server_name _;
    #     
    #     ssl_certificate /etc/nginx/ssl/cert.pem;
    #     ssl_certificate_key /etc/nginx/ssl/key.pem;
    #     
    #     location / {
    #         proxy_pass http://yt_shorts_backend;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
